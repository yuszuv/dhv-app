// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "de.sternprodukt.dhv"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
